

# 系统级别的配置
# application 用户级别配置
# 中专
spring:
  cloud:
    config:
      # git 上面的配置文件名，不加后缀哦
      name: config_file/application
      # 配置l
      profile: dev
      # 节点
      label: master
      uri: http://localhost:9500
